# Используем официальный образ PHP с веб-сервером
FROM php:8.2-cli

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем исходный код
COPY index.php .

# Открываем порт 8000
EXPOSE 8000

# Запускаем встроенный PHP-сервер
CMD ["php", "-S", "0.0.0.0:8000"]